class BBox {
  double top;
  double bottom;
  double right;
  double left;

  BBox({double top, double bottom, double right, double left})
      : top = top,
        bottom = bottom,
        right = right,
        left = left;
}

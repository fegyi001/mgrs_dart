class BBox {
  double top;
  double bottom;
  double right;
  double left;

  BBox({
    required double top,
    required double bottom,
    required double right,
    required double left,
  })   : top = top,
        bottom = bottom,
        right = right,
        left = left;
}
